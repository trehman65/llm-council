services:
  # Backend API Service
  - type: web
    name: llm-council-backend
    runtime: python
    buildCommand: uv sync && uv pip install --system -e .
    startCommand: uv run uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11
    healthCheckPath: /
    plan: free

# Note: Frontend must be deployed separately as a Static Site via Render Dashboard
# See DEPLOY.md for detailed instructions

